# Minimal dependencies for the judge/TikTok inference pipeline.
# Install torch/torchvision from the wheel index appropriate for the runtime.

torch>=2.4
torchvision>=0.19
timm>=1.0.11
peft>=0.13.0
pillow>=10.4
numpy>=1.26
